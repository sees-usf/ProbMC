    current_x1 = Bool("current_x1")
    next_x1 = Bool("next_x1")
    current_x2 = Bool("current_x2")
    next_x2 = Bool("next_x2")
    current_x3 = Bool("current_x3")
    next_x3 = Bool("next_x3")

    property = (And(next_x1==True, next_x2==False, next_x3==True))  # As of right now, we have to negate the property ourselves
    # Note: next_x1==True, next_x2==False, and next_x3==True on step 4

    property_probability = 1